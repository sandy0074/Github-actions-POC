name: Pull request Merge-event Workflow

on:
  push:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: Deploy-step
        run: |
          echo 'This step to Deploy the code'